# Convert Second to Minutes & Hours
seconds = 7200
minutes = seconds // 60
hours = minutes // 60
print(minutes, 'minutes in {} seconds'.format(seconds))
print(hours, 'hours in {} seconds'.format(seconds))
